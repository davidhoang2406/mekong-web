server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — all non-file routes resolve to index.html.
    # API/WS routing, CORS, auth, and rate limiting are handled by Kong.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
